# API com Laravel para realizar consulta de CEPS

Para executar o projeto faça o clone e os seguintes comandos:
```
./vendor/bin/sail up
```

## Rotas
 - GET: /api/search/local/{cep}: O parâmetro cep pode ser um ou mais ceps, separados por vírgulas.
